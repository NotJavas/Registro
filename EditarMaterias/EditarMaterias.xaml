<Window x:Class="Registro.EditarMaterias.EditarMaterias"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestionar Materias" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#37393f">
    <Grid Margin="15">
        <DataGrid x:Name="MateriasGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="➕ Agregar Nuevo" Click="MenuNuevo_Click"/>
                    <MenuItem Header="📝 Editar Seleccionado" Click="MenuEditar_Click"/>
                    <MenuItem Header="❌ Eliminar Seleccionado" Click="MenuEliminar_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50" />
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="2*" />
                <DataGridTextColumn Header="Semestre" Binding="{Binding Semestre}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>